---
mapping:
  rules:
  - local:
      user:
        ext:
          value:
          - "{0}"
          - "{2}"
          multiValue: true
        domain: "{D}"
        name: "{D}"
        email: "{D}"
        roles:
        - "{D}"
        - "{1}"
        expire: "{D}"
      extGroup:
        ext2:
          value:
          - "{0}"
          - "{2}"
          multiValue: true
    remote:
    - name: ext2
      multiValue: true
    - name: roles2
      multiValue: true
    - name: ext3
      multiValue: true
  version: RAX-1